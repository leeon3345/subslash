<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- 오래된 웹뷰에서 상태 표시줄·내비게이션 바 뒤로 보이는 색. window_colors.xml 참고. -->
        <item name="android:windowBackground">@color/subslash_window_background</item>
    </style>


    <!-- 실행 화면. 안드로이드 11 이하는 splash.png를, 12 이상은 바탕색과 적응형 아이콘 전경을 그린다.
         둘 다 웹 manifest의 background_color(#09090B)와 앱 아이콘을 따른다(scripts/generate-brand-assets.mjs). -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">#09090B</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>